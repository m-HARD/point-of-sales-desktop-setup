# سجلّ التغييرات — نقطة بيع

كل إصدارٍ منشور وما فيه. الأحدث أوّلاً.

الأرقام تتبع [التسمية الدلالية](https://semver.org/lang/ar/): `كبير.صغير.إصلاح`.

---

## v1.2.1 — طلباتٌ لا تُرفض بلا داعٍ، ورسائلُ فشلٍ لا تصمت

> **لمن يستخدم جهازاً فرعياً:** حدّث من الشريط في أعلى شاشة الربط.

نسخةٌ وقائية: لا تُغيّر شيئاً في الاستخدام اليومي، وتمنع عطبين ظهرا أثناء
تشغيل جهازٍ فرعيّ.

### ما أُصلح

**طلبٌ يُرفض لثوانٍ بعد التثبيت.** محرّك التطبيق قد يحتاج نصف دقيقة ليبدأ
عمله بعد كل تثبيت — غالباً بسبب فحص مضادّ الفيروسات. والطلبات في تلك الفترة
كانت تُرفض بلا إعادة محاولة، فيظهر خطأٌ غامض لمن ضغط زرّاً مبكراً. صارت
الطلبات التي لا تحمل بيانات تُعاد على محرّكٍ آخر بدل أن تفشل.

**ورسائلُ الفشل صارت تقول أين وقع العطب.** كان بعض الأخطاء يظهر بجملةٍ عامّة
لا يُعرف منها أالعطبُ في هذا الجهاز أم في الجهاز الرئيسيّ. الآن يظهر تحتها
سطرٌ صغير يقول ذلك صراحةً.

### لمن يثبّت جهازاً فرعياً لأوّل مرة

بعد التثبيت، أمهل التطبيق دقيقةً قبل الضغط على «فتح نظام نقطة البيع».

---

## v1.2.0 — فتحُ نظام الرئيسيّ، ورسائلُ فشلٍ تقول السبب

> **لمن يستخدم جهازاً فرعياً:** حدّث من الشريط في أعلى شاشة الربط.

### ما أُصلح

**زرّ «فتح نظام نقطة البيع».** كان يعتمد على تفاوضٍ بين الشاشة والخادم على شكل
الردّ، وذلك التفاوض يسقط أحياناً بلا أثر. صار للعنوان مسارٌ مستقلّ يردّه نصاً
دائماً، فتنتقل الشاشةُ به بنفسها.

**ورسائلُ الفشل تقول السبب.** كان كلُّ تعذّرٍ في الوصول إلى الجهاز الرئيسيّ
يظهر بجملةٍ واحدة — سواءٌ رفضَ الرئيسيُّ الرمز، أو لم يُفتح الاتصال أصلاً، أو
ردّ بخطأ. وثلاثتُها تُعالَج بثلاث طرقٍ مختلفة.

الآن يظهر تحت الرسالة سطرٌ صغير يقول ما حدث بالضبط.

### وأمرٌ يخصّ الترقية

صار الخادمُ يُغلق تماماً قبل بدء التثبيت. وويندوز لا يسمح باستبدال ملفٍ مفتوح،
فبدءُ المثبّت قبل الإغلاق التامّ كان يعرّض بعض الملفات للتخطّي.

### لمن يستخدم جهازاً فرعياً

بعد الترقية: اضغط «فتح نظام نقطة البيع» من شاشة الربط. وإن لم تنتقل النافذة،
فالسطر الصغير تحت الرسالة يقول لماذا — أرسله كما هو.

---

## v1.1.9 — فتحُ نظام الرئيسيّ بلا وسيط

> **لمن يستخدم جهازاً فرعياً:** حدّث من الشريط في أعلى شاشة الربط.

### ما أُصلح

زرّ «فتح نظام نقطة البيع» كان يعتمد على أن يفهم المتصفّح إشارةَ انتقالٍ ويتصرّف
بها. وحين لا يفعل، لا يحدث شيء عند الضغط — ولا رسالة تُقرأ.

صارت الشاشةُ تسأل عن عنوان الجهاز الرئيسيّ صراحةً، ثم تنتقل إليه بنفسها. وإن
تعذّر الوصول، ظهرت رسالةٌ تقول السبب بدل صمت.

### لمن يستخدم جهازاً فرعياً

بعد الترقية: اضغط «فتح نظام نقطة البيع» من شاشة الربط. يجب أن تنتقل النافذة إلى
واجهة الجهاز الرئيسيّ.

### وأمرٌ يخصّ الترقية نفسها

كان المثبّت يبدأ عملَه قبل أن تُغلق عمليات الخادم إغلاقاً تاماً. وويندوز لا
يسمح باستبدال ملفٍ مفتوح، فكانت بعضُ ملفات التطبيق تبقى كما هي بينما يتقدّم
رقمُ النسخة — أي ترقيةٌ تبدو ناجحة ولا تُغيّر شيئاً.

صار الخادم يُغلق تماماً قبل بدء التثبيت.

> **مهمّ لهذه المرّة وحدها:** أغلق التطبيق تماماً قبل تشغيل المثبّت. الإصلاح
> نفسه لم يصل نسختك الحالية بعد.

---

## v1.1.8 — شاشة الربط تحمل شريطَ نسخةٍ وتحديث

> **لمن يستخدم جهازاً فرعياً: ثبّت هذه النسخة يدوياً مرّةً واحدة.** نزّل المثبّت
> من هنا وشغّله فوق النسخة الحالية — لا تحذف التطبيق. ترخيصُك وربطُك باقيان.
> وبعدها يصبح التحديث من داخل شاشة الربط نفسها.

### ما أُضيف

شريطٌ في أعلى شاشة الربط يعرض اسم التطبيق ونسخته. وبالضغط عليه تظهر بطاقة
التحديث كاملةً — التحقّق، والتنزيل بنسبته، والإيقاف، والتثبيت — ومعها نسخةُ
الجهاز الرئيسيّ للمقارنة.

وإن كان ثمّة تحديثٌ متاح، فتح الشريط نفسه مرّةً واحدة وقال ذلك.

### لماذا

الجهاز الفرعيّ لا يرى من التطبيق إلا شاشة الربط: لا قائمة، ولا إعدادات، ولا
تسجيل دخول. وزرُّ التحديث كان يعيش في «عن البرنامج» وحدها — أي خلف بابٍ مغلق.

فجهازٌ فرعيّ عالقٌ على نسخةٍ فيها عطبٌ لم يكن يملك طريقاً إلى إصلاح نفسه، ولا
يبقى إلا حذفُ التطبيق وتنزيلُه يدوياً.

---

## v1.1.7 — الجهاز الفرعيّ يفتح نظام الرئيسيّ

> **لمن يثبّت لأوّل مرّة:** ثبّت `1.1.7` مباشرةً.

### ما أُصلح

**زرّ «فتح نظام نقطة البيع» لم يكن ينقل النافذة.** يقترن الجهاز الفرعيّ بنجاح،
وتقول الشاشة إنه مربوط، ثم لا يحدث شيءٌ عند الضغط — بلا رسالة ولا خطأ. صار
الانتقال يتمّ كما ينبغي.

**والجهاز الرئيسيّ لم يكن يعرض عنوانه على الشبكة.** كانت لوحةُ الأجهزة تقول
«تعذّر تحديد عنوان الجهاز» على جهازٍ متّصلٍ فعلاً، فيستحيل الاقترانُ اليدويّ —
وهو المخرج الوحيد حين يعجز البحثُ التلقائي عن الرئيسيّ. صار العنوان يظهر.

### لمن يستخدم جهازاً فرعياً

بعد الترقية: افتح شاشة الربط على الفرعيّ، واضغط «فتح نظام نقطة البيع».

وإن أردت الاقتران يدوياً، فعنوانُ الجهاز الرئيسيّ معروضٌ الآن في
**الإعدادات ← الأجهزة** على الرئيسيّ نفسه.

---

## v1.1.6 — التطبيق يكتب هويّة الجهاز في سجلّه

> **لمن يثبّت لأوّل مرّة:** ثبّت `1.1.6` مباشرةً.

هذه نسخةٌ تشخيصية لا تُغيّر سلوكاً.

### ما أُضيف

عند كل تشغيل يكتب التطبيق في سجلّه سطراً يقول ما يراه عن نفسه: نوعُ الترخيص
(رئيسيّ أو فرعيّ)، ومساحةُ العمل، والنسخة، وحالةُ الترخيص.

### لماذا

جهازٌ فرعيّ عند أحد المستخدمين كان يفتح شاشة تسجيل الدخول بدل شاشة الربط. وكلُّ
فحصٍ يقول إن إعداداته سليمة، ومع ذلك لا يعمل كما ينبغي.

ولم يكن في السجلّ ما يقول ما رآه التطبيق **لحظة القرار** — فتعذّر تحديد السبب
عن بُعد.

الآن يكفي إرسالُ ملفّ السجلّ لتحديد الخلل بلا تجارب.

### أين السجلّ

`%APPDATA%\Point of Sales\storage\logs\laravel.log`

---

## v1.1.5 — الجهاز الفرعي يعمل حتى لو نقص إعدادٌ في ملفّه

> **لمن يثبّت لأوّل مرّة:** ثبّت `1.1.5` مباشرةً.
>
> **ولمن جرّب ربط جهازٍ فرعي فلم يعمل: حدّث الجهاز الفرعي — لا حاجة لحذف
> التطبيق أو إعادة تفعيل الترخيص.**

### ما كان يحدث

جهازٌ بترخيصٍ فرعيّ صحيح، ونوعُه مسجَّل صحيحاً في التطبيق — ومع ذلك يفتح شاشة
تسجيل الدخول ويعمل ببياناته الخاصة، بدل شاشة الربط.

ولا رسالة خطأ في أي مكان: كلُّ شيءٍ يبدو سليماً.

### السبب

الوضع الفرعي كان يعتمد على إعدادٍ يُكتب في ملفّ إعدادات التطبيق عند التثبيت.
والتثبيت النظيف يكتبه، والترقية تُضيفه — لكن جهازاً واحداً يفوته لأي سبب (ملفٌّ
لم يُحفظ، أو صلاحيةٌ منعت الكتابة، أو تعديلٌ يدويّ أزال السطر) يفقد الوضع
الفرعي بصمت.

### ما صار الآن

الإعداد لم يعد يعتمد على ذلك الملف وحده — يُمرَّر للتطبيق في كل تشغيل، فلا
يمكن أن يفوت.

ومن أراد تعطيل استقبال الأجهزة الفرعية على جهازٍ بعينه، فتعديلُه اليدويّ ما زال
محترماً كما كان.

---

## v1.1.4 — الترخيص الفرعي كان يُسجَّل جهازاً رئيسياً

> **لمن يثبّت لأوّل مرّة:** ثبّت `1.1.4` مباشرةً.
>
> **ولمن جرّب ربط جهازٍ فرعي ولم ينجح: هذا هو الإصلاح.**

### ما كان يحدث

تُدخل ترخيصاً فرعياً في جهازٍ ثانٍ، فيقبله التطبيق — ثم يعمل الجهاز **كأنه
رئيسيّ**: يفتح بياناته الخاصة، ويبيع فيها، ولا يظهر شيء منها على جهاز المتجر.
والربطُ يتمّ ظاهرياً ولا يغيّر شيئاً.

ولا رسالة خطأ في أي مكان — الجهاز «يعمل»، لكنه يعمل وحده.

### السبب

كلُّ ترخيص يحمل بداخله — بتوقيعٍ لا يُزوَّر — نوعَه: رئيسيّ أو فرعيّ. وكان
التطبيق يقرأ النوع من ردّ خادم التراخيص بدل المفتاح نفسه، فإن لم يذكره الردّ
اعتبر الجهاز **رئيسياً**.

فترخيصٌ فرعيّ صحيح تماماً يُسجَّل جهازاً رئيسياً بصمت.

### ما صار الآن

النوع يُقرأ من الترخيص نفسه، وهو المصدر الوحيد الذي لا يحتمل الخطأ.

**والأجهزة التي سُجّلت خطأً تُصحَّح تلقائياً** عند أوّل تشغيل بعد التحديث — بلا
إعادة تفعيل ولا إعادة ربط. حدِّث الجهاز الفرعي، وستجده يفتح شاشة الربط كما
ينبغي.

### كيف تتأكّد

بعد تحديث الجهاز الفرعي وفتحه:

- **أوّل شاشة يجب أن تكون شاشة الربط** — لا شاشة تسجيل الدخول. فإن ظهرت شاشة
  الدخول فالجهاز ما زال مسجَّلاً رئيسياً.
- وبعد الربط، اضغط «فتح نظام نقطة البيع» — تنتقل إلى شاشات جهاز المتجر
  وبياناته.
- وعلى الجهاز الرئيسي: الإعدادات ← الأجهزة، يظهر الجهاز **متصلاً**.

---

## v1.1.3 — الجهاز الفرعي يعمل فعلاً، وتنزيل التحديث يقول أين بلغ

> **لمن يثبّت لأوّل مرّة:** ثبّت `1.1.3` مباشرةً.
>
> **ولمن يستعمل أجهزةً فرعية: حدِّث الجهاز الرئيسي والفرعي معاً.**

### الجهاز الفرعي كان يعمل على نفسه بعد الربط

هذا أهمّ ما في الإصدار. الربط ينجح، ثم يُكمل الجهاز الفرعي بشاشاته وبياناته
الخاصة بدل بيانات المتجر. وأربعة أعراضٍ بدت مستقلّة كانت كلُّها من هذا:

- الفرعي يبيع في قاعدةٍ لا يراها أحد، ولا تظهر مبيعاته على الجهاز الرئيسي.
- والجهاز الرئيسي يعرضه **مفصولاً** رغم أن الربط تمّ.
- وإغلاق الجهاز الرئيسي لا يوقف الفرعي.
- وضغطُ «فصل الجهاز» من اللوحة لا يصله.

السبب أن الربط كان يرفع القيد عن الجهاز الفرعي بدل أن يوجّهه إلى الرئيسي.

**الآن:** الجهاز الفرعي لا يفتح شيئاً من عنده — لا صفحة دخول ولا غيرها. شاشةُ
الربط فقط، وبعد الربط يظهر فيها زرّ **«فتح نظام نقطة البيع»** الذي ينقله إلى
الجهاز الرئيسي. وكلُّ ما بعد ذلك يُعرض من هناك: بياناتٌ واحدة، ومخزونٌ واحد.

وعليه فالفصلُ من اللوحة يصله فوراً، وإغلاقُ الرئيسي يوقفه، ويظهر «متصل» ما دام
يعمل.

> **إن كان لديك جهازٌ فرعي مربوط من قبل:** حدّثه، وستجده يعود إلى شاشة الربط.
> اضغط «فتح نظام نقطة البيع» ولا حاجة لإعادة الاقتران.

### وتنزيل التحديث لم يعد صامتاً

كان الزرّ يُعطَّل ولا شيء يتغيّر حتى ينتهي تنزيلُ أكثر من مئة ميغابايت — فيبدو
التطبيق معلَّقاً.

صار يعرض **النسبة داخل الزرّ**، وشريط تقدّم، والحجم المنقول من الإجمالي،
والسرعة. ومعه زرّ **«إيقاف التنزيل»** لمن بدأه في وقتٍ لا يناسبه.

ويمكنك متابعة العمل أثناء التنزيل. وإن انقطع الاتصال يبدأ التنزيل من جديد عند
إعادة المحاولة — وهذا مكتوبٌ في الشاشة نفسها كي لا تنتظر استئنافاً لا يقع.

---

## v1.1.2 — طرق الدفع عند البيع، وتنبيهٌ كان يكذب

> **لمن يثبّت لأوّل مرّة:** ثبّت `1.1.2` مباشرةً.

### نافذة إتمام البيع صارت تتبع طرق الدفع التي أدخلتها

كانت الخيارات ثابتة: نقد، واسمُ أوّل تطبيق بنكي، وهجين. فمن أضاف بطاقةً لم يجد
لها مدخلاً، ومن أضاف تطبيقاً بنكياً ثانياً وجد الشاشة تسمّي الصنف كلَّه باسم
واحدٍ منه.

صارت الخيارات تُشتقّ مما أدخلته:

- **نقدي** — دائماً.
- **تطبيق بنكي** — إن كان لديك تطبيقٌ واحد على الأقل، ثم تختار أيّها.
- **بطاقة** — إن كانت لديك بطاقة، ثم تختار أيّها. ولا تختلط بالتطبيقات.
- **هجين** — إن كان لديك أيُّ طريقةٍ غير نقدية.

### والهجين صار حرّاً

دفعتان، كلُّ واحدةٍ بأيّ طريقة: نقد وبطاقة، أو تطبيقين مختلفين، أو **التطبيق
نفسه مرّتين** برقمَي عملية مختلفين — وهي حالةٌ تقع فعلاً حين يُحوّل العميل خطأً
ثم يُكمل بتحويلٍ ثانٍ. لكلِّ دفعةٍ مبلغُها ورقمُ عمليّتها.

ودفعتان لا أكثر: التقسيم على ثلاثٍ يقع نادراً، وحقولٌ لا تُملأ تُبطئ كلّ بيعة.

### تنبيه «تغيّرت البيانات» كان يظهر بلا سبب

ظهر لمن **هو نفسه** من غيّر البيانات: يضيف طريقة دفع، فيُقال له إن جهازاً آخر
غيّرها. ولم يعد يختفي.

وصار لا يظهر إلا حين يكون له معنى: على جهازٍ مرتبطٍ به جهازٌ فرعيّ فعّال، أو
على جهازٍ فرعيّ. أما الجهاز الوحيد فلا كاتبَ معه — فلا سؤال ولا تنبيه.

### ورسالة «لم يُعثر على جهاز رئيسي»

كانت سطراً واحداً لا يدلّ على شيء، فظنّ من قرأها أن الربط يحتاج سلكَ شبكة.
صارت تُسمّي الأسباب مرتَّبةً: **أن يكون الجهاز الرئيسي محدَّثاً** (النسخُ
الأقدم لا تستقبل أجهزةً فرعية أصلاً)، وأن التطبيق مفتوحٌ عليه، وأن الجهازين على
الشبكة نفسها — والواي‑فاي يكفي، ونقطةُ اتصال الهاتف تكفي — وأن ويندوز سمح
للتطبيق، مع موضع الزرّ الذي يمنح الإذن.

---

## v1.1.1 — «خدمات التأمين» للصيدليات وحدها

> **لمن يثبّت لأوّل مرّة:** ثبّت `1.1.1` مباشرةً.

كانت تظهر في **الإعدادات ← سياسات البيع** لكل الأنواع، وهي تخصّ الصيدليات: جهةُ
التأمين تتحمّل جزءاً من قيمة الوصفة ويُسوّى معها لاحقاً — ولا يقع ذلك في متجرٍ
أو سوبرماركت.

صارت لا تظهر إلا في تراخيص الصيدلية.

وإعلانُ ميزةٍ «قريباً» لمن لن تصله أسوأ من إخفائها: ينتظرها، ثم يسأل عنها، ثم
يظنّ أن نسخته ناقصة.

**وفُحصت بقيّة الوسوم المشابهة:** زرُّ «البدائل» في شاشة البيع مقصورٌ على
الصيدليات أصلاً، وطرقُ الدفع لا تخصّ نوعاً بعينه. فالتسريب كان واحداً.

---

## v1.1.0 — الشاشة تعرف أن البيانات تغيّرت

> **لمن يثبّت لأوّل مرّة:** ثبّت `1.1.0` مباشرةً.

### ما كان يحدث

صفحةٌ تُركت مفتوحة لا تعلم بما تغيّر تحتها. كاشيرٌ يفتح شاشة البيع، وآخرُ يبيع
آخر قطعةٍ من صنف — والأوّل ما زال يرى الكمّية القديمة.

### ما صار الآن

يتحقّق التطبيق كلَّ عشر ثوانٍ **من رقمٍ واحد** لا من الصفحة كلّها. والغالب أن
يتطابق، فلا يحدث شيء البتّة: لا بطء، ولا وميض، ولا حقلٌ يُفقد.

وحين تتغيّر البيانات فعلاً:

- **شاشة البيع** تُحدَّث صامتةً — الأسعار والكمّيات تصير الأحدث بلا أن يفعل
  الكاشير شيئاً، ودون أن تتأثّر السلّة أو البحث.
- **باقي الشاشات** يظهر فيها تنبيهٌ هادئ في الزاوية: **«تغيّرت البيانات من جهاز
  آخر — تحديث»**. لا يحجب شيئاً، ولا يُحدِّث حتى تضغط — فلا يضيع ما أدخلته في
  منتصف عمل.

والتنبيه يفرّق بين ما يعنيك: بيعةٌ على جهازٍ آخر لا تُزعجك وأنت في الإعدادات.

### اختلاف النسخة بين الأجهزة

سؤالٌ يُطرح دائماً: هل يجب أن تكون الأجهزة على نسخةٍ واحدة؟

**يُستحسن، وليس شرطاً.** الجهاز الفرعي يعرض صفحات الرئيسي، فمعظم الاختلافات لا
تُلاحظ. لكن بعض الميزات — كالطباعة — تعتمد على نسخة الجهاز نفسه، فقد تتوقّف على
الأقدم منهما بلا رسالة.

صارت **شاشة الربط تعرض رقمَي النسخة معاً** وتنبّه إن اختلفا. والقاعدة العملية:
حدِّث الأجهزة كلَّها بعد تحديث أيٍّ منها.

### ورقم النسخة

انتقل الترقيم إلى `1.1.0`: الخانة الوسطى للتحديثات المتوسطة، وهذا واحد منها.

---

## v1.0.10 — انقطاع الجهاز الرئيسي يُشرح بالعربية

> **لمن يثبّت لأوّل مرّة:** ثبّت `1.0.10` مباشرةً.

### ما كان يحدث

الجهاز الفرعي يعرض شاشات الجهاز الرئيسي. فإن أُطفئ الرئيسيُّ أو انقطعت الشبكة
والكاشير ينقر أيَّ رابط، كان يظهر خطأُ المتصفّح نفسه: بالإنجليزية، بمصطلحاتٍ
تقنية، وبلا زرٍّ يخرج منه.

### ما صار الآن

يعود الجهاز الفرعي تلقائياً إلى شاشته، ويقول بوضوح:

> **انقطع الاتصال بالجهاز الرئيسي**
> الربط ما زال قائماً — لا حاجة لإعادته. تأكّد من أن الجهاز الرئيسي يعمل ومن
> اتصال الشبكة، ثم اضغط «فتح نظام نقطة البيع».

والتمييز مقصود: لولاه لرأى الكاشير شاشة ربطٍ عادية فظنّ أن الربط انفكّ، وأعاد
الإجراء كلَّه — بينما الجهاز الرئيسي مطفأٌ فحسب.

ومعها تذكيرٌ بما يهمّ فعلاً: **ما لم يُحفظ قبل الانقطاع لم يُحفظ.** الجهاز
الفرعي لا يحتفظ بشيء محلياً، وهذا ما يمنع نشوء مخزونين متضاربين.

---

## v1.0.9 — الخادم يخدم الأجهزة معاً لا بالتناوب

> **لمن يثبّت لأوّل مرّة:** ثبّت `1.0.9` مباشرةً.

### ما كان يحدث

الجهاز الرئيسي كان يخدم طلباً واحداً في كل لحظة. فتقريرٌ ثقيل أو نسخةٌ
احتياطية عليه تُجمّد الأجهزة الفرعية حتى تنتهي، وجهازان يعملان معاً يتناوبان
بلا داعٍ.

### ما صار الآن

يخدم الرئيسيُّ أربعة طلباتٍ في وقتٍ واحد. القياس على ثمانية طلباتٍ متزامنة:
كانت تستغرق **أكثر من سبعة أضعاف** زمن الطلب الواحد، وصارت **ضعفين ونصفاً**.

عملياً: تقريرٌ يُفتح على الرئيسي لم يعد يوقف الكاشير على الجهاز الآخر.

### وما لزم معه

الخدمةُ المتوازية تكشف ما كان الطلبُ الواحد يُخفيه، فعولج قبل أن يظهر عندك:

- **تنافسُ الكتابة على قاعدة البيانات.** صارت تُنظَّم بحيث ينتظر الطلبُ دوره
  بدل أن يرتدّ بخطأ. سابقاً كان ثلاثةٌ من كل ستّين طلباً متزامناً يفشلون؛ الآن
  **ستّون من ستّين تنجح**.
- **النسخُ الاحتياطي.** كان يُؤخذ بنسخ ملف القاعدة، وقد يكتب فيه جهازٌ آخر
  أثناء ذلك فتخرج نسخةٌ ممزّقة **تُفتح ولا يُشتكى منها** — ولا يُكتشف الخلل إلا
  يوم يُحتاج إليها. صار يُؤخذ بطريقةٍ تضمن اتّساقها مهما جرى حولها.
- **عدّادُ محاولات الدخول.** كان يمكن أن يرى الأجهزةَ كلَّها جهازاً واحداً،
  فيقفل خطأُ كاشيرٍ الدخولَ على الجميع. صار لكل جهازٍ عدّاده.

### إذن جدار الحماية

يسأل ويندوز مرّةً واحدة عند أوّل استماع على الشبكة. ومن ضغط «لا» — أو غاب عن
السؤال فأُغلق — لم تكن النافذة تعود أبداً، ويفشل ربطُ الأجهزة بعدها بلا سببٍ
ظاهر.

صارت **الإعدادات ← الأجهزة** تعرض التنبيه وزرّ «السماح للأجهزة الفرعية» متى
كان الإذن ناقصاً. والإذن محصورٌ في الشبكات الخاصة — شبكة متجرك لا الشبكات
العامة.

### ملاحظة

عدد العمّال أربعة، وهو يناسب حتى خمسة أجهزة. ويستهلك ذاكرةً أكثر قليلاً من
النسخة السابقة — وهو ثمنُ ألا ينتظر أحد.

---

## v1.0.8 — الأجهزة الفرعية تعمل

> **لمن يثبّت لأوّل مرّة:** ثبّت `1.0.8` مباشرةً.

### ما كان يحدث

تُدخل ترخيصاً فرعياً في جهازٍ ثانٍ، فيفتح صفحة تسجيل الدخول ويعمل كأنه جهاز
رئيسيّ مستقلّ — بلا ربطٍ بجهاز المتجر، وببياناتٍ خاصّةٍ به.

### السبب

آليّةُ الربط كانت مبنيّةً بالكامل — بوابةُ الربط، والبحث عن الجهاز الرئيسي في
الشبكة، والتصريحُ المسبق، ونقلُ الجلسة — لكنّها كانت **مُطفأة**، ومعها عطبٌ
ثانٍ يمنعها من العمل حتى لو أُضيئت: الجهاز الرئيسي كان يستمع على نفسه فقط، على
منفذٍ يتغيّر في كل تشغيل. فلا الفرعيُّ يصل إليه، ولا يعرف أين يبحث عنه.

### ما صار الآن

- الجهاز الفرعي **لا يفتح شيئاً** قبل الربط: لا صفحة دخول ولا صفحات أخرى، بل
  شاشةُ الربط وحدها.
- الجهاز الرئيسي صار مسموعاً على شبكة المتجر بمنفذٍ ثابت، فيجده الفرعيُّ
  بالبحث التلقائي.
- ولوحةُ «الأجهزة» على الرئيسي تعرض **عنوانه على الشبكة** — يُكتب يدوياً على
  الفرعيّ إن تعذّر البحث التلقائي، وهو ما يحدث في بعض شبكات المتاجر.
- ويُضاف إذنُ جدار الحماية عند التثبيت، محصوراً في **الشبكات الخاصة** — شبكةِ
  المتجر لا الشبكات العامة.

### كيف تربط جهازاً

1. على الرئيسي: **الإعدادات ← الأجهزة ← إصدار رمز إذن**.
2. على الفرعي: اختر الجهاز من قائمة الأجهزة المكتشفة، أو اكتب عنوانه المعروض
   في لوحة الرئيسي.
3. اكتب الرمز قبل انتهاء مهلته.

الرمز إذنٌ مسبق منك، لا طلبُ انضمام. فلا يصل أحدٌ إلى بيانات متجرك ولو عرف
عنوان الجهاز.

### ملاحظتان

**الجهاز الرئيسي يجب أن يبقى مفتوحاً** ما دام الفرعيُّ يعمل — الفرعيّ يعرض
بيانات الرئيسي ولا يحتفظ بنسخةٍ منها.

**والخدمة بالتناوب:** الرئيسيّ يخدم طلباً واحداً في كل لحظة. الاستعمال العاديّ
لا يُلاحَظ فيه ذلك، لكن تقريراً ثقيلاً أو نسخةً احتياطية على الرئيسي سيُبطئ
الفرعيَّ حتى تنتهي. تجنّب تشغيلها في وقت الذروة.

### وأيضاً

من أراد جهازاً معزولاً عن الشبكة تماماً، يمكن تعطيل الاستماع — والتطبيق يعمل
كما كان، ويتعذّر ربط الأجهزة الفرعية.

---

## v1.0.7 — زرّ التحديث يعمل من جديد

> **لمن يثبّت لأوّل مرّة:** ثبّت `1.0.7` مباشرةً.

هذا التثبيت **يدويّ لآخر مرّة**. بعده يعمل التحديث من داخل التطبيق.

### ما كان يحدث

عند الضغط على «تحديث» في صفحة «عن البرنامج» تظهر رسالة:

```
ENOENT: no such file or directory, open '...\resources\app-update.yml'
```

فلا سبيل إلى التحديث إلا بتنزيل المثبّت من صفحة الإصدارات يدوياً.

### السبب

داخل التطبيق ملفٌّ صغير يخبر آليّةَ التحديث من أين تسأل عن الإصدارات. وطريقةُ
البناء التي اعتُمدت منذ `1.0.1` — وهي التي أصلحت أيقونة سطح المكتب — كانت
تتخطّى الخطوة التي تكتبه.

فخرج كلُّ مثبّتٍ منذ ذلك الحين سليماً في كل شيء إلا التحديث. ولم يظهر العطب
لأن كل تثبيتٍ بعده جرى يدوياً، فلم يمرّ أحدٌ بمسار التحديث.

صار الملف يُكتب في كل بناء، ويُتحقَّق من وجوده مرّتين — والبناء **يتوقّف** إن
غاب، بدل أن يُنتج مثبّتاً لا يستطيع التحديث.

---

## v1.0.6 — التطبيق يُقلع دائماً، وأشرطة الفلترة في سطر

> **لمن يثبّت لأوّل مرّة:** ثبّت `1.0.6` مباشرةً — لا يعنيك ما دونها.

يجمع هذا الإصدار ما لم يُنشر من `1.0.4` و`1.0.5`.

### التطبيق كان يعلق أحياناً عند شاشة تشغيل الخادم

إن أُغلق التطبيق إغلاقاً غير نظيف — انقطاعُ كهرباء، أو إنهاءٌ من مدير المهام،
أو انهيار — تبقى عمليةُ خادمٍ حيّةً في الخلفية تحجز ذاكرةً لا يستطيع التشغيل
التالي أخذها. فتنتظر شاشةُ الإقلاع ثلاثين ثانية ثم تستسلم، بلا رسالة خطأ ولا
سبب.

صار التطبيق يتعرّف على تلك العملية عند الإقلاع ويُنهيها — عمليّاتِه هو وحدها،
لا أيَّ برنامجٍ آخر على الجهاز. ومعها طريقةٌ بديلة لحجز الذاكرة إن تعذّر
الحجزُ الأصليّ، فلا يتوقّف الإقلاع لهذا السبب مرّةً أخرى.

### وإن مات الخادم في منتصف العمل، يعود

كان موتُ الخادم يعني زرّاً لا يستجيب: تضغط «تسجيل الدخول» أو «إتمام البيع»
فلا يحدث شيء، ولا سبيل إلا إغلاق البرنامج وفتحه.

صار التطبيق يُشغّله من جديد خلال أقلّ من ثانية — والكاشير في منتصف بيعةٍ لا
يفقدها. ويُكتب سببُ الانقطاع في سجلّ التطبيق كي يُعالَج جذرُه.

### أشرطة الفلترة كانت تتمدّد على عدّة أسطر

في **التقارير** و**الحسابات والجرد** و**الفواتير والسجل**، كان كلُّ حقلِ فلترةٍ
يأخذ سطراً كاملاً — فيشغل الشريط نصف الشاشة قبل أن يظهر أيُّ محتوى.

صارت الحقول تصطفّ في سطرٍ واحد على الشاشات المعتادة، وارتفاعُ الشريط خُمسُ ما
كان. وعلى شاشةٍ ضيّقة تلتفّ إلى سطرين بدل خمسة.

---

## v1.0.3 — إصلاح الشاشة البيضاء

> **لمن يثبّت لأوّل مرّة:** ثبّت `1.0.3` مباشرةً.

بعد أن زال «Server Error» في `1.0.2` ظهرت **شاشةٌ بيضاء بلا رسالة خطأ**.

سببها ملفٌ صغير من بيئة التطوير (`public/hot`) تسلّل إلى الحزمة، فصار التطبيق
يطلب ملفّاته من خادم تطويرٍ غير موجود على جهاز العميل: الصفحة تُفتح، ولا يُحمَّل
منها شيء، ولا يُكتب خطأٌ في أي سجلّ.

صار البناء يحذفه، **ويرفض إنتاج مثبّت إن وُجد** — فحصٌ عند البوّابة، لأن خطأً
صامتاً عند العميل أغلى من بناءٍ يتوقّف.

### وعنوان النافذة

كان ينتهي بكلمة «Laravel» بدل اسم البرنامج.

---

## v1.0.2 — إصلاح «Server Error» الثاني

> **لمن يثبّت لأوّل مرّة:** لا يعنيك ما هنا — ثبّت `1.0.2` مباشرةً.

`1.0.1` أصلح شقّاً من عطب الترقية وبقي شقٌّ ثانٍ يُظهر «Server Error» نفسه.

جدول بيئات العمل يعيش في قاعدة بيانات النظام، ولا تصله ترحيلات التطبيق — فأعمدته
تُضاف بقائمةٍ خاصّة. وقد سقط منها عمودٌ أُضيف لاحقاً، وحارسُ الترخيص يكتبه عند
كل طلب، فسقط كل طلب.

صار العمود يُضاف ذاتياً، ومعه فحصٌ يقارن القائمة بما تحتاجه الجداول فعلاً حتى
لا يسقط عمودٌ آخر مستقبلاً. وأُجريت بروفةُ ترقيةٍ على نسخةٍ من قاعدة بياناتٍ
حقيقية قبل الإصدار.

**بياناتك سليمة في الحالتين** — العطب كان في بنية جدولٍ لا في محتواه.

---

## v1.0.1 — إصلاح الترقية والأيقونة

إصلاحان ظهرا عند أوّل ترقيةٍ حقيقية من `0.2.8`.

> **لمن يثبّت لأوّل مرّة:** لا يعنيك شيءٌ ممّا هنا — ثبّت `1.0.1` مباشرةً.

### خطأ «Server Error» بعد الترقية

قاعدةُ بيئة العمل تحمل سجلّاً بالترحيلات المطبَّقة عليها، وقد يفترق هذا السجلّ
عن حال الجداول فعلاً. وحين افترق، أعاد التطبيق تشغيل ترحيلاتٍ مطبَّقةٍ سلفاً
فاصطدم أوّلها بجدولٍ موجود — وسقط التطبيق كلّه: لا شاشة بيع ولا إعدادات.

صارت هذه الترحيلات **تفحص قبل أن تُنشئ**، فإعادتُها على قاعدةٍ تحملها لا تفعل
شيئاً وتمرّ. ولا تُفقد بياناتٌ في الحالتين.

### أيقونة سطح المكتب

كان الاختصار يحمل شعار Electron بدل شعار التطبيق. الآن يحمله في سطح المكتب
وشريط المهام والمثبّت ونافذة البرنامج.

### واسم المتجر الافتراضي

خرج في `1.0.0` مشوَّهاً (`Ù†Ù‚Ø·Ø©…`) بسبب خطأ ترميزٍ في أداة البناء. من غيّر
اسم متجره من الإعدادات لم يتأثّر.

---

## v1.0.0 — الإصدار الأوّل للنشر

أوّل إصدارٍ يُنشر للاستخدام. ما قبله (`0.x`) كان بناءً داخلياً للتجربة، وهذا
يختلف عنه في الواجهات والجداول اختلافاً واسعاً — فهو ليس ترقيةً بل بداية.

> **التثبيت:** حمّل المثبّت وشغّله. التطبيق يُنشئ قاعدة بياناته ويطبّق جداولها
> ذاتياً عند أوّل تشغيل وعند كل تحديثٍ بعده، فلا خطوةَ إعدادٍ يدوية.

### نقطة البيع

- **الدواء منتهي الصلاحية لم يعد يُباع بصمت.** كان اختيار الدفعة تلقائياً
  (الأقرب انتهاءً أوّلاً) يختار المنتهية بنفسه ويمرّ البيع. صار البيع يُرفض
  برسالةٍ تسمّي الصنف وتاريخه.
- **زرّ «البدائل» في وضع الصيدلية يعمل** — يقترح ما يشترك في المادة الفعّالة،
  الأقرب صلاحيةً أوّلاً، ولا يقترح منتهياً ولا نافداً. والنقر عليه يضعه في
  السلة.
- **بحثٌ يتسامح مع خطأ الحرف**: «اموكسسيلين» تجد «أموكسيسيلين»، و«الباراسيتامول»
  تجد «باراسيتامول». يوحّد صور الهمزة والتاء المربوطة والتشكيل وأل التعريف.
- طرق الدفع صارت بياناتٍ تُدار من الإعدادات: أضِف بنكاً أو محفظةً وتظهر في كل
  شاشة. ووجهةُ ردّ المرتجع صارت اختياراً — فاتورةٌ دُفعت تحويلاً قد تُردّ نقداً.
- تنبيهٌ عند إغلاق الوردية بوجود مسودات مهجورة.

### المخزون

- **شاشتان جديدتان ظاهرتان**: «قارب النفاد» و«قاربت الصلاحية». العتبة لكل صنفٍ
  عتبتُه، والترتيب بالأشدّ نقصاً.
- **التسعير السريع** (اختياري من سياسات البيع): تغيير أسعارٍ كثيرة في شاشةٍ
  واحدة — سعرٌ لكل صنف أو نسبةٌ على المحدَّد — مع معاينةٍ تُظهر الفروق ووسمِ ما
  ينزل تحت التكلفة، وتنفيذٍ في معاملةٍ واحدة.
- خصمُ المخزون يوزَّع على عدة دفعات عند الحاجة، وبصمةُ التكلفة تتبع الدفعات
  المستهلكة فعلاً.
- سعرُ الوحدة الأعلى (الكرتون) يتبع سعر الأساس عند كل تغيير.

### الحسابات والتقارير

- **أرقام «الحسابات والجرد» تعدّ كل شيء مرّةً واحدة.** كان ردُّ المرتجع يُخصم
  من الإيراد ويُضاف للتكاليف، وسدادُ المورد يُحسب فوق فاتورة الشراء، وتكلفةُ
  المشتريات تنخفض عند تسجيل شراء. صُحّحت الأربعة.
- تقرير الموردين صار يقول ممّ تكوّن رقمُه: أعلى الموردين وفواتير الفترة.
- **الفواتير المكتملة متجمّدة**: تغييرُ اسم صنفٍ أو سعره أو حذفه لا يمسّ فاتورةً
  سابقة ولا ربحَ فترةٍ ماضية.

### الطباعة

- **آليةٌ واحدة** بدل خمس. زينةُ التطبيق لا تُطبع، ولا صفحاتٌ فارغة.
- **اختيار مقاس الورق** قبل الطباعة (A4 · A5 · حراري ٨٠مم) — التقارير ليست
  إيصالات. ويُحفظ الاختيار لكل نوع مستند.
- «طباعة السجل» في سجلَّي الديون والمحفظة تطبع السجلّ كلّه لا الصفحة المعروضة،
  وعلى الورقة ترويسةٌ تقول المتجر والفلتر والتاريخ.

### الإعدادات

- **العملات**: قائمة عملاتٍ مدعومة، وقفلٌ يمنع إفساد الدفاتر بتغييرٍ متأخّر،
  وتقليلُ الخانات العشرية متاحٌ بإقرارٍ صريح.
- إعدادات البرنامج صارت تفضيلاتٍ لكل مستخدم على جهازه (المظهر، اللوحة، حجم
  الخط، التقويم، الساعة) بلا زرّ حفظ.
- **النسخ الاحتياطي**: مسار التخزين يُفحص أثناء الكتابة على الخادم مع عرض
  المساحة الحرّة، والصفحة تبدأ بالفعل لا بالإعداد.
- **ضبط المصنع يُنهي ما يبدأ**: التفضيلات المحلية وجلسات الجرد وطرق الدفع
  والمستخدمون.

### الهوية والوصول

- خمس لوحات ألوان تعمل في كل شاشة — أُزيل مئةٌ وثمانيةٌ وأربعون لوناً مثبَّتاً
  كان يتجاهل اختيار المستخدم.
- سلّم عناوين وحوافّ وأسطح موحّد، وحلقةُ تركيز لكل حقل، وصناديق اختيارٍ تتبع
  الثيم.
- شريط ترقيم الصفحات بالعربية.
- **الوضع الليلي**: نصوصٌ كانت تختفي صارت مقروءة.
- الصلاحيات مُدقَّقة: لوحة الصيدلية للمالك، والكاشير لا يرى أرقام المخزون.

### إصلاحات

- اختيار الطابعة كان يسقط لأن جدول إعدادات الجهاز جُمّد بشكلٍ أقدم.
- ترحيلٌ فاشل كان يُجمّد بيئة العمل عند نصف جداولها بصمت.
- حذفُ مستخدمٍ كان يمحو فواتيره معه.
- طباعةُ الفاتورة كانت تخرج ورقةً بيضاء بعد زيارة صفحة المنتجات.
- رمزُ العملة كان يُطبع خاطئاً على فاتورة الزبون في بعض المسارات.

### للمطوّر

- سلسلة اختبارات: **٦٥٠** اختباراً · ٣٠٧٧ تحقّقاً.
- رقمُ النسخة صار مصدراً واحداً: القشرة تكتبه في بيئة التطبيق عند كل إقلاع.

---

## ما قبل v1.0.0

إصدارات `0.1.0` – `0.2.8` كانت بناءً داخلياً للتجربة ولم تُنشر للاستخدام.
تفاصيلها في أوصاف الإصدارات على GitHub.
