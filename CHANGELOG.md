# سجلّ التغييرات — نقطة بيع

كل إصدارٍ منشور وما فيه. الأحدث أوّلاً.

الأرقام تتبع [التسمية الدلالية](https://semver.org/lang/ar/): `كبير.صغير.إصلاح`.

---

## v1.0.0 — الإصدار الأوّل للنشر

أوّل إصدارٍ يُنشر للاستخدام. ما قبله (`0.x`) كان بناءً داخلياً للتجربة، وهذا
يختلف عنه في الواجهات والجداول اختلافاً واسعاً — فهو ليس ترقيةً بل بداية.

> **التثبيت:** حمّل المثبّت وشغّله. التطبيق يُنشئ قاعدة بياناته ويطبّق جداولها
> ذاتياً عند أوّل تشغيل وعند كل تحديثٍ بعده، فلا خطوةَ إعدادٍ يدوية.

### نقطة البيع

- **الدواء منتهي الصلاحية لم يعد يُباع بصمت.** كان اختيار الدفعة تلقائياً
  (الأقرب انتهاءً أوّلاً) يختار المنتهية بنفسه ويمرّ البيع. صار البيع يُرفض
  برسالةٍ تسمّي الصنف وتاريخه.
- **زرّ «البدائل» في وضع الصيدلية يعمل** — يقترح ما يشترك في المادة الفعّالة،
  الأقرب صلاحيةً أوّلاً، ولا يقترح منتهياً ولا نافداً. والنقر عليه يضعه في
  السلة.
- **بحثٌ يتسامح مع خطأ الحرف**: «اموكسسيلين» تجد «أموكسيسيلين»، و«الباراسيتامول»
  تجد «باراسيتامول». يوحّد صور الهمزة والتاء المربوطة والتشكيل وأل التعريف.
- طرق الدفع صارت بياناتٍ تُدار من الإعدادات: أضِف بنكاً أو محفظةً وتظهر في كل
  شاشة. ووجهةُ ردّ المرتجع صارت اختياراً — فاتورةٌ دُفعت تحويلاً قد تُردّ نقداً.
- تنبيهٌ عند إغلاق الوردية بوجود مسودات مهجورة.

### المخزون

- **شاشتان جديدتان ظاهرتان**: «قارب النفاد» و«قاربت الصلاحية». العتبة لكل صنفٍ
  عتبتُه، والترتيب بالأشدّ نقصاً.
- **التسعير السريع** (اختياري من سياسات البيع): تغيير أسعارٍ كثيرة في شاشةٍ
  واحدة — سعرٌ لكل صنف أو نسبةٌ على المحدَّد — مع معاينةٍ تُظهر الفروق ووسمِ ما
  ينزل تحت التكلفة، وتنفيذٍ في معاملةٍ واحدة.
- خصمُ المخزون يوزَّع على عدة دفعات عند الحاجة، وبصمةُ التكلفة تتبع الدفعات
  المستهلكة فعلاً.
- سعرُ الوحدة الأعلى (الكرتون) يتبع سعر الأساس عند كل تغيير.

### الحسابات والتقارير

- **أرقام «الحسابات والجرد» تعدّ كل شيء مرّةً واحدة.** كان ردُّ المرتجع يُخصم
  من الإيراد ويُضاف للتكاليف، وسدادُ المورد يُحسب فوق فاتورة الشراء، وتكلفةُ
  المشتريات تنخفض عند تسجيل شراء. صُحّحت الأربعة.
- تقرير الموردين صار يقول ممّ تكوّن رقمُه: أعلى الموردين وفواتير الفترة.
- **الفواتير المكتملة متجمّدة**: تغييرُ اسم صنفٍ أو سعره أو حذفه لا يمسّ فاتورةً
  سابقة ولا ربحَ فترةٍ ماضية.

### الطباعة

- **آليةٌ واحدة** بدل خمس. زينةُ التطبيق لا تُطبع، ولا صفحاتٌ فارغة.
- **اختيار مقاس الورق** قبل الطباعة (A4 · A5 · حراري ٨٠مم) — التقارير ليست
  إيصالات. ويُحفظ الاختيار لكل نوع مستند.
- «طباعة السجل» في سجلَّي الديون والمحفظة تطبع السجلّ كلّه لا الصفحة المعروضة،
  وعلى الورقة ترويسةٌ تقول المتجر والفلتر والتاريخ.

### الإعدادات

- **العملات**: قائمة عملاتٍ مدعومة، وقفلٌ يمنع إفساد الدفاتر بتغييرٍ متأخّر،
  وتقليلُ الخانات العشرية متاحٌ بإقرارٍ صريح.
- إعدادات البرنامج صارت تفضيلاتٍ لكل مستخدم على جهازه (المظهر، اللوحة، حجم
  الخط، التقويم، الساعة) بلا زرّ حفظ.
- **النسخ الاحتياطي**: مسار التخزين يُفحص أثناء الكتابة على الخادم مع عرض
  المساحة الحرّة، والصفحة تبدأ بالفعل لا بالإعداد.
- **ضبط المصنع يُنهي ما يبدأ**: التفضيلات المحلية وجلسات الجرد وطرق الدفع
  والمستخدمون.

### الهوية والوصول

- خمس لوحات ألوان تعمل في كل شاشة — أُزيل مئةٌ وثمانيةٌ وأربعون لوناً مثبَّتاً
  كان يتجاهل اختيار المستخدم.
- سلّم عناوين وحوافّ وأسطح موحّد، وحلقةُ تركيز لكل حقل، وصناديق اختيارٍ تتبع
  الثيم.
- شريط ترقيم الصفحات بالعربية.
- **الوضع الليلي**: نصوصٌ كانت تختفي صارت مقروءة.
- الصلاحيات مُدقَّقة: لوحة الصيدلية للمالك، والكاشير لا يرى أرقام المخزون.

### إصلاحات

- اختيار الطابعة كان يسقط لأن جدول إعدادات الجهاز جُمّد بشكلٍ أقدم.
- ترحيلٌ فاشل كان يُجمّد بيئة العمل عند نصف جداولها بصمت.
- حذفُ مستخدمٍ كان يمحو فواتيره معه.
- طباعةُ الفاتورة كانت تخرج ورقةً بيضاء بعد زيارة صفحة المنتجات.
- رمزُ العملة كان يُطبع خاطئاً على فاتورة الزبون في بعض المسارات.

### للمطوّر

- سلسلة اختبارات: **٦٥٠** اختباراً · ٣٠٧٧ تحقّقاً.
- رقمُ النسخة صار مصدراً واحداً: القشرة تكتبه في بيئة التطبيق عند كل إقلاع.

---

## ما قبل v1.0.0

إصدارات `0.1.0` – `0.2.8` كانت بناءً داخلياً للتجربة ولم تُنشر للاستخدام.
تفاصيلها في أوصاف الإصدارات على GitHub.
